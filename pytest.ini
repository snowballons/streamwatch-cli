[pytest]
minversion = 6.0
addopts = -ra -q --cov=src/streamwatch --cov-report=term-missing --cov-report=html --cov-fail-under=30
testpaths =
    tests
pythonpath =
    src
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow running tests
